export default function DashboardHeader() {
  return (
    <div className="mb-8">
      <h1 className="text-3xl font-bold text-foreground">Job Import Dashboard</h1>
      <p className="mt-2 text-base text-muted-foreground">
        Monitor job import runs from external job feeds and track import history
      </p>
    </div>
  )
}
